"""ORDER Intelligence: аналитика для руководителя.

Пакет, а не один файл: аналитика перестала быть одной сводкой. Разделы
здесь по вопросам, на которые они отвечают, а не по слоям кода.

    facts.py           заявки на внимание, этапы, объекты, люди, дубли,
                       тренды, счётчики — то, с чего аналитика началась
    executive.py       сводка руководителя одним числом на показатель
    stale.py           заявки без движения и нормативы этапов (SLA)
    inconsistencies.py нестыковки: чего в заявке не хватает
    anomalies.py       что отличается от обычного уровня
    attention.py       единая очередь «требует внимания»
    scope.py           границы видимости (RBAC) до обращения к модели
    intents.py         разрешённый набор запросов для «Спросить ORDER AI»

Главное правило, общее для всего пакета: **считает сервер, объясняет
модель**. Ни одна функция здесь не ходит в Anthropic. Числа собираются
запросами к базе, а `services/intelligence.py` отдаёт их модели текстом и
получает объяснение. Иначе в сводке появятся правдоподобные выдуманные
цифры, а по ним распоряжаются деньгами.

Второе правило: функции чистые и про HTTP не знают. Поэтому их одинаково
зовут роутер, фоновая задача (утренняя сводка) и бот.

Имена из `facts` реэкспортируются: до фазы 5 модуль назывался
`services/analytics.py`, и переучивать вызывающих ради переезда файла
незачем.
"""

from app.services.analytics.facts import (  # noqa: F401
    BLIND_SPOTS,
    CRITICAL_FACTOR,
    DUPLICATE_WINDOW_HOURS,
    IN_WORK_LIMIT,
    STAGES,
    UNUSUAL_AMOUNT_FACTOR,
    attention_list,
    digest_facts,
    find_duplicates,
    in_work_briefs,
    people_stats,
    project_stats,
    stage_stats,
    totals,
    trends,
)
