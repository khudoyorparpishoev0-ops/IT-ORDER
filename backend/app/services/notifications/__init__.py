"""Кому, когда и куда отправить.

Пакет, а не файл: уведомлений стало три рода, и у них разная природа.

    requests.py      судьба заявки — подана, согласована, оплачена
    intelligence.py  сводки руководителю и сигналы о критичном
    dedup.py         защита от повторов и история доставок
    delivery.py      сама отправка в Telegram, устойчивая к сбоям

Разделение обязанностей между слоями жёсткое:

* `services/analytics/` отвечает на вопрос **что произошло**. Он ничего
  не отправляет и про получателей не знает;
* этот пакет отвечает на вопрос **кому, когда и куда**. Он ничего не
  считает и в базу заявок за цифрами не ходит.

Смешать их — значит однажды получить сводку, которая считает по-своему,
потому что «так удобнее было отправить».

Имена из `requests` реэкспортируются: до фазы 5C модуль назывался
`services/notifications.py`, и переучивать одиннадцать вызывающих ради
переезда файла незачем.
"""

from app.services.notifications.requests import (  # noqa: F401
    notify_new_request,
    notify_priced,
    notify_sourcing,
    panel_url,
    request_url,
)
