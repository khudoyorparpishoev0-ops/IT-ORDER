-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
# Нужен только для scripts/build_pdf_fonts.py, в образ не попадает
fonttools[woff]==4.55.3
# Читает готовый PDF в тестах: проверять надо то, что увидит человек, а
# не свои же черновики. В образ не попадает.
pypdfium2==5.13.0
